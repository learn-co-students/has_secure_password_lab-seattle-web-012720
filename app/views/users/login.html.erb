<h1> Login Form </h1>
<%= form_for @user, :url => url_for(:controller => 'sessions', :action => 'create')do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit "Login"%>
<% end %>
